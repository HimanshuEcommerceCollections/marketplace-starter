/** Optional brand metadata seed (titles/OG). Swap for real values before prod. */
export const apexMetadata = {
  titleTemplate: "%s | Apex Total Home Services",
  description: "Trusted pros for every job around the home. (Sample)",
  ogImage: "/og-image.svg",
};

/** Optional brand metadata seed (titles/OG). Swap for real values before prod. */
export const eventsMetadata = {
  titleTemplate: "%s | Events & Media",
  description: "Plan, capture, and produce unforgettable events. (Sample)",
  ogImage: "/og-image.svg",
};
